{
    "tools": [
        {
            "name": "senseleak",
            "binary_path": "/var/crates/infer/infer/scan",
            "run": [
                "./{binary_path} --repo={code_path} -v --pretty --report={output_path}"
            ]
        }
    ]
}