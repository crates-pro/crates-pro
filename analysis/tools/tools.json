{
    "tools": [
        {
            "name": "senseleak",
            "binary_path": "build/sensleak-rs/scan",
            "run": [
                "./{binary_path} --repo={code_path} -v --pretty --report={output_path}"
            ]
        }
    ]
}